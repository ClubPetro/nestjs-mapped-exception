{
  "name": "nest-mapped-exception",
  "version": "0.0.0",
  "description": "Mapped Exceptions for NestJS",
  "main": "dist/index.js",
  "types": "dist/index",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && tsc",
    "prepublish": "npm run build"
  },
  "keywords": [
    "nestjs",
    "mapped",
    "exception"
  ],
  "author": "Jhow Rodrigues",
  "license": "MIT",
  "dependencies": {
    "@nestjs/common": "^7.0.13",
    "rxjs": "^6.5.5"
  },
  "devDependencies": {
    "@types/node": "^14.0.4",
    "rimraf": "^3.0.2",
    "typescript": "^3.9.3"
  }
}
